#pragma once

#include <iostream>

#include "Partition.h"

using namespace std;

namespace Partition_Test {
	bool full();
	
	bool addSubPartitions();

	bool mergePartitions();

	bool getNPartitions();

	bool getNSubPartitions();
}